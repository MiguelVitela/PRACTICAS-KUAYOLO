/* Continuando con el juego del carro será necesario crear una función que incremente las vueltas
completadas*/

let vueltasCompletadas = 0;

// Crea una función que incremente las vueltas completdas y llámala 3 veces.

function incremento(){
    vueltasCompletadas +=1
    console.log(vueltasCompletadas)
}


incremento()

incremento()

incremento()
